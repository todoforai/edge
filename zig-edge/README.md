# zig-edge (compatibility location)

The active Zig implementation has moved to:

- `edge/bridge/zig`

Use that directory as the source of truth for the TODOforAI Bridge Zig runtime.

This folder remains temporarily for compatibility with existing docs/scripts.
